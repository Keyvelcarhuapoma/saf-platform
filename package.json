{
  "name": "saf-platform-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd noc-dashboard && npm run dev",
    "build": "cd noc-dashboard && npm install && npm run build",
    "start": "cd noc-dashboard && npm run start"
  }
}
